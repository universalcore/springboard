language: python
python:
  - "2.7"
services:
  - elasticsearch
install:
  - pip install --upgrade pip
  - pip install -r requirements-dev.txt
  - pip install -e .
script:
  - py.test springboard
after_success:
  - coveralls
